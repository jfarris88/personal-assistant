# Reference Documents

*Central index of saved local reference documents such as PDF exports, email archives, and other files worth keeping for later.*

## Saved Documents

| Added | Title | Type | Source | Why Keep | Stored File | Supporting Note | First Noted |
|-------|-------|------|--------|----------|-------------|-----------------|-------------|
| 2026-06-17 | ChatGPT Access for All Employees via Okta | Email PDF | Dean Roman, 2026-06-16 | Company-wide ChatGPT rollout announcement with Okta access details, usage guidance reminder, and IT support contacts | [2026-06-16-chatgpt-access-all-employees-okta-email.pdf](../reference/emails/2026-06-16-chatgpt-access-all-employees-okta-email.pdf) | [ChatGPT Okta Rollout Email](../notes/chatgpt-okta-rollout-email.md) | [2026-06-17 Daily Notes](../daily/2026-06-17.md) |
| 2026-06-25 | O'Reilly DevDocs | Local git clone | Engineering org | Central internal Engineering docs: platform architecture, runbooks, auth, accounts/users, security, infra, monitoring. Primary research source for IT triage and Jira ticket enrichment. | `~/src/devdocs/devdocs/docs/` (local) · live: https://devdocs.common-build.gcp.oreilly.com/ | [Jira AI Enrichment](../notes/jira-ai-enrichment.md) | 2026-06-25 |

| 2026-06-29 | Create a read-only database user using create_ro_database_user.sh | Google Doc (needs relocation) | Justin Breninger, last updated 2024-09-24 | Runbook for provisioning read-only DB users via `create_ro_database_user.sh` — covers prereqs (Homebrew, gcloud-cli, kubectl, psql, jq, orm-cli) and usage. Urgent issues: contact Devin Cooley or `#engineering-general` | [inbound/Create a read-only database user using create_ro_database_user.sh.md](../inbound/Create%20a%20read-only%20database%20user%20using%20create_ro_database_user.sh.md) · [Google Doc](https://docs.google.com/document/d/1XgwrQGx0ucy1gEMSzPcFXjAl-CgNQNweh08TycWZQVA/edit?tab=t.0) | **Needs relocation to Confluence** (target: IT/Infra space, ideally the new IT team space planned for Atlassian Cloud migration) — see [migration candidates list](../notes/confluence-cloud-migration-candidates.md) | 2026-06-29 |
| 2026-07-09 | Adding SSH keys for Linux system authentication | Confluence page (Data Center — needs relocation) | Space IS | How to add an SSH public key to your Okta profile for key-based auth (Linux systems, CDN SFTP access, etc.). Important/high-traffic reference doc. | Live: [pageId 79135041](https://intranet.oreilly.com/confluence/pages/viewpage.action?pageId=79135041) | [cdn-access-process.md](../notes/cdn-access-process.md) · [migration candidates list](../notes/confluence-cloud-migration-candidates.md) | 2026-07-09 |
| 2026-07-10 | gdpr-priv-automation | GitHub repo | `oreillymedia/gdpr-priv-automation` | Python/shell automation that collects current user emails from PDB/FileMaker/ExactTarget and closes linked GDPR PRIV Jira tickets when a requested email is confirmed gone. On24/OLT are not among its automated closers — those are still manual. | Live: [github.com/oreillymedia/gdpr-priv-automation](https://github.com/oreillymedia/gdpr-priv-automation) | [on24-anonymization-automation.md](../notes/on24-anonymization-automation.md) | 2026-07-10 |
| 2026-07-10 | On24 Anonymization Rollout (Jay's copy) | Google Doc | Copied by Jay so it could be shared with the `gcp-helpdesk-prod-dsadmin` service account | Rollout plan doc for the On24 anonymization automation effort with Flex 4/U&A | [Google Doc](https://docs.google.com/document/d/1BqBnYuVb81O_VgFd-adzJPbHD1nC5t2S3A12WSslsYc/edit) | [on24-anonymization-automation.md](../notes/on24-anonymization-automation.md) | 2026-07-10 |
| 2026-07-14 | 1:1 Notes — Dean Roman (manager) | Google Doc | "Dean-Jay 1:1", one running doc with dated section headings (newest near top) | Source for manager 1:1 action items during triage/briefings | [Google Doc](https://docs.google.com/document/d/1tQC_dLsX13GfrcOJ-TNdx0H4FLGIlcyJhfByTSTz2_I/edit) | — | 2026-07-14 |
| 2026-07-14 | 1:1 Notes — Elton Lee | Google Doc | "Jay-Elton 1:1 Notes" | Source for direct-report 1:1 action items | [Google Doc](https://docs.google.com/document/d/13PEjeCl6PXJvqh-xrC9RuKEC-PkN-wOz7rt0UB07-N8/edit) | — | 2026-07-14 |
| 2026-07-14 | 1:1 Notes — Jemrey Paraluman | Google Doc | "Jemrey - Jay 1:1" | Source for direct-report 1:1 action items | [Google Doc](https://docs.google.com/document/d/1qW1jcdOHh6KxpGuMf4sNevQNRaD-RsxkosWUCEDAK2Y/edit) | — | 2026-07-14 |
| 2026-07-14 | 1:1 Notes — Michael Seneschal | Google Doc | "Jay-Michael 1:1" (an older, superseded doc of the same name is dated Nov 2025 — use the one actively updated, most recently Jun 26 2026) | Source for direct-report 1:1 action items | [Google Doc](https://docs.google.com/document/d/1pGIM3I7GGbF09N1Fldzp3St03djr4CMaY_F0nHkydIo/edit) | — | 2026-07-14 |
| 2026-07-23 | AI Strategy — Working copy (all departments) | Google Sheet | Jay's working copy, shared with `gcp-helpdesk-prod-dsadmin` SA | Tracks Claude/AI-tool assignment strategy per department. Current tabs: AI Systems, Use Cases, Accounting and Finance, Legal, HR, **Product Management** (added 2026-07-23), **Latest Roster** (Jay added this, full HRIS export in-sheet — see below), **Current Claude Users** (populated 2026-07-23 via the live Claude Admin API — all 198 org members, Name/Email/Role/Added At). Product Management tab: 38 people from the Latest Roster tab, grouped into 4 sections by manager (Boutté/Data Science-King/Product Management incl. Gonzalez's team/Paterson Project Mgmt & QA). Cross-check vs. Current Claude Users (2026-07-23): **26 of the 38 already have Claude; 12 don't** — the 5-person Data Science team (King) has none, plus Diane Gleeson, Shawn Storc, Joshua Burt's whole QA team (Bavand/Karpov/Margolina/Zich). | [Google Sheet](https://docs.google.com/spreadsheets/d/1WY4GDVcnlAFXf-9lQlkK6ORHtjm7nWHBPg75vFEkTGs/edit) | [current_projects.md #2](current_projects.md) | 2026-07-23 |
| 2026-07-23 | AI Strategy — Sales Teams template (example breakout) | Google Sheet | Referenced by Jay as the model for splitting a department into its own sheet with team sub-tabs | Example of the target end-state (per-department sheet, broken into teams) once a department tab graduates out of the working copy. **Not yet shared with the SA — 403 when fetched 2026-07-23.** Ask Jay to share with `gcp-helpdesk-prod-dsadmin@helpdesk-prod-1.iam.gserviceaccount.com` before relying on it as a template. | [Google Sheet](https://docs.google.com/spreadsheets/d/1xGA3oea2aUCr2qzIbehg-GYYkL-p0UKbssN9UIdCV_M/edit) | [current_projects.md #2](current_projects.md) | 2026-07-23 |
| 2026-07-23 | Adhoc userlist (HRIS roster export) | Google Sheet | Jay's ad hoc tracking sheet, shared with `gcp-helpdesk-prod-dsadmin` SA | Multi-purpose tracking sheet. Key tab: **"Latest Roster"** — full HRIS export (381 employees, 37 columns: name, division, department, job title, manager, email, status, etc.) — the authoritative source for building department/team rosters. Other tabs: KnowBe4, Claude AD group, Nooks users, Granola, Claude Subscriptions, UK & JP Email Roster, Claude Engineering AD group, Claude All AD Groups, Claude Product AD group. | [Google Sheet](https://docs.google.com/spreadsheets/d/1qV46CW_vHH9YtCr0pIsY77tK7cIxnCPB_TJKvLNmOP0/edit) | [current_projects.md #2](current_projects.md) | 2026-07-23 |

## Tools & Systems

- **Cortex (getcortexapp.com)** — O'Reilly's software/service catalog & engineering intelligence platform (replacing Surveyor/Metabase). Tracks service/repo ownership via `cortex.yaml`, integrates GitHub/Datadog/Slack, and has "scorecards" for standards compliance. **API key: "Cortex API (read)"** in 1Password Employee vault (username `jfarris-cli`, created 2026-07-02) — Jay's personal read-only key. **Use for:** looking up service/repo ownership or team context during helpdesk/IT triage and research requests. Distinct from the separate Cortex service-account key requested under [AM-2301](https://intranet.oreilly.com/jira/browse/AM-2301) (Theo Moser's `signal-iq` leadership dashboard, still Pending Approval, meant for GCP Secret Manager — not this key). Extent of intended use still TBD per Jay (2026-07-02).

**Verified working 2026-07-02** — API base `https://api.getcortexapp.com`, auth via `Authorization: Bearer <key>`. Confirmed use case: the **SOC2 Compliance scorecard** (`GET /api/v1/scorecards/soc2-compliance`, scores at `.../soc2-compliance/scores`) is the live "report" of repos subject to SOC2 — scope = all `service`-type catalog entities *except* those tagged with the `exclude-from-soc2` group (336 in scope / 52 excluded as of 2026-07-02). Checks vuln thresholds + branch protection/required approvals. Runbook: `devdocs.common-build.gcp.oreilly.com/operations/security/soc.html#provide-a-list-of-projects-that-are-included-in-the-platform`.

**Known API limitation — "what does team X own?" queries (found 2026-07-02):** There's no working server-side filter for this with the read key. `GET /api/v1/catalog` never returns per-entity `ownersV2`/`owners` data (always `null`) regardless of query params tried (`ownerTeamTag`, `owningTeamTags`, `teamTag`, `team`, `includes=owners` — all silently ignored, still return the full 421-entity catalog). The real CQL endpoint (`POST /api/v1/queries`) returns **403 Forbidden** with this key — no query-builder access, catalog/scorecard reads only. **Working approach:** individual entity detail (`GET /api/v1/catalog/{tag}`) *does* include full `ownersV2.teams`, so the only way found so far is: page through `/api/v1/catalog` for all entity tags, then hit `/api/v1/catalog/{tag}` per entity (N+1, ~421 calls as of 2026-07-02) and filter client-side for the team tag (e.g. `oreillymedia-metacon`). Works but slow — fine for a one-off, not for frequent/repeated use. Team tag format is the Cortex `teamTag` (e.g. `oreillymedia-metacon`), **not** the git/idpGroup slug (e.g. `oreillymedia/metacon`) — those differ and only the former matches `ownersV2.teams[].tag`. If asked again, re-check whether Cortex has since added a working filter/bulk-by-team endpoint before repeating the N+1 workaround.

- **Chassis service admin access (Groot/Django groups)** — Chassis-based GCP services (e.g. `<service>.platform.gcp.oreilly.com/admin`) control admin access via Groot-synced Django Groups, not self-service. Granting access requires the service's owning team (find via Cortex) to create/confirm a permissions Group, then `@guardian-devs` in Slack to add users to it. Full process + example (SOL-106969 / feedback-service): [chassis-service-admin-access-process.md](../notes/chassis-service-admin-access-process.md).

- **1Password CLI (`op`) for `.env` secrets** — standing pattern for keeping real credentials out of `.env` files: store the secret in a 1Password item/vault, reference it in `.env` as `op://vault/item/field` (or `op://vault/item/filename` for file-type secrets like service account JSON keys), then run the app via `op run --env-file=.env -- <command>` so secrets resolve only in-process, never on disk. Local-dev-only pattern — once a project is hosted on Google Cloud, secrets move to Google Secret Manager instead. Full generic step-by-step: [op-cli-env-secrets-guide.md](../notes/op-cli-env-secrets-guide.md).

- **Claude Admin API — pulling org user list programmatically** (confirmed working 2026-07-22) — Anthropic's Admin API endpoint `GET https://api.anthropic.com/v1/organizations/users` (header `x-api-key: <key>`, `anthropic-version: 2023-06-01`, paginate via `limit`/`after_id`/`has_more`/`last_id`) returns each member's id/email/name/role/added_at. **Key used:** 1Password item "Claude AI (Anthropic) - Team Plan" (Helpdesk_Procurement vault), field "API Key - Members and Groups Read Only" — a scoped key created via the org console's Keys UI (Org settings > API-ish "Keys" section, "+ Create key") with scopes `read:members` + `read:rbac_groups`, prefix `sk-ant-api01-...` (**not** the classic `sk-ant-admin01-...` Admin API key format — this is a separate, newer scoped-key system). **Gotcha:** a freshly created key 401s ("invalid x-api-key") for a minute or two before it propagates — retry rather than assuming the key is bad. **Not yet found:** a working endpoint for the `read:rbac_groups` scope itself — tried `/v1/organizations/{groups,rbac_groups,user_groups,roles}` and the separate Compliance API path `/v1/compliance/groups`, all 404/403. If a groups-listing need comes up again, re-check docs.claude.com before re-guessing paths. There is also a distinct **Compliance API** (`/v1/compliance/organizations/{org_uuid}/users`, scopes `read:compliance_user_data`/`read:compliance_org_data`) — a third, separate key/endpoint system from both the classic Admin API and this scoped-members-key system; don't conflate the three.
- **Claude billing/analytics visibility — Team vs. Enterprise plan** (confirmed 2026-07-10, re: AM-2431) — O'Reilly is currently on Claude's **Team** plan (144 seats, per Org settings > Billing screenshot 2026-07-10), with an Enterprise upgrade being considered but not yet done.
  - **On Team plan (current state):** No custom roles. Only **Owner/Primary Owner** can see Org settings > Billing (spend). **Admin** can manage members but cannot see Billing. There is no way to grant someone view-only spend/billing access without making them a full Owner.
  - **On Enterprise plan (after upgrade):** Custom roles become available — you can create a role with Billing → "Can view" and Analytics → "Can view" while leaving Identity & Access / User Management / Privacy / Libraries at "No access", then assign it via a Group (Org settings > Roles, Org settings > Groups). This is the path to give an exec view-only reporting access without full admin. Enterprise also exposes a read-only **Analytics API** for pulling usage data into external dashboards. No API exists to *provision* roles/permissions themselves on either plan — role setup is UI-only via Org settings.
  - **Implication for AM-2431 (Julie Baron):** on the current Team plan, the only way to give her billing/reporting visibility is to make her Owner or Primary Owner (full access, not scoped) — there's no view-only middle ground until the Enterprise migration happens.

- **Superanswers — internal engineering docs/decisions corpus** (set up 2026-07-20, via [AM-2460](https://intranet.oreilly.com/jira/browse/AM-2460)) — O'Reilly's org-wide ADR/proposal/decisions site built by Doug Hogan's team on GitHub Pages (`oreillymedia/superanswers` repo content, powered by the `oreillymedia/markdown-comments` tool). As of setup: 397 docs, 34 decisions, 16 open questions, spanning engineering/leadership/PM audiences. Live site: gated behind a GitHub App auth layer, two current instance URLs are `legendary-adventure-zgemyyk.pages.github.io` and `effective-adventure-qj272gj.pages.github.io` (random GitHub Pages hostnames — a branded `*.internal-docs.oreilly.com` subdomain is in progress, see [current_projects.md](current_projects.md) project #16).
  - **Set up for Claude Code use:** marketplace `oreillymedia/orm-claude-dev-marketplace` registered, plugin `orm-claude-doc-graph@orm-claude-dev-marketplace` installed (user scope) — ships `orm-claude-doc-graph-explore` (read-only corpus queries: "show me docs about X", "what's the state of thinking on Y", open decisions/questions, author activity) plus authoring/comment/density skills for writing back to the corpus.
  - **Local checkout:** `~/code/superanswers` (cloned via `gh`-authenticated `git clone`, account `jfarris88`). The explore skill regenerates `_data/graph.json` etc. from this checkout on demand and checks it against the remote for staleness before answering.
  - **Use for:** researching prior engineering decisions/architecture proposals/analyses when Jay wants a "second brain" style answer sourced from what O'Reilly engineering has actually written, rather than general knowledge.

- **GitHub org administration — Helpdesk vs. IT Operations Systems Admin (corrected 2026-07-23, re: SOL-107219 / ITOPS-45345)** — Both **Helpdesk** (Jay's team) and **IT Operations Systems Admin** (a separate ITOPS sub-team) hold GitHub org admin/owner rights for `oreillymedia`. But only **Systems Admin does hands-on repo workflow configuration** (branch protection rules, merge queue behavior, required checks, connectors, DVCS integrations, etc.) for other teams' repos — Helpdesk has the org-admin permission but does not do this work. **David Buckley (`dbuckley`, ITOPS)** is the confirmed pattern-match assignee for this category — Jira history shows him owning both GitHub repo-config tickets ([ITOPS-44600](https://intranet.oreilly.com/jira/browse/ITOPS-44600) "Fix GitHub connector in itops-playground", [ITOPS-43454](https://intranet.oreilly.com/jira/browse/ITOPS-43454) "Jira-GitHub DVCS Account Integration Error", [ITOPS-44705](https://intranet.oreilly.com/jira/browse/ITOPS-44705) "Create github-us-central1 connection in orm-finance-prod") *and* DAP-specific infra work ([ITOPS-44211](https://intranet.oreilly.com/jira/browse/ITOPS-44211) "DAP Delivery Platform: Cloud Run service + Pub/Sub infrastructure", On Hold). SOL tickets in this space get mirrored into an ITOPS-key ticket automatically (SOL-107219 → [ITOPS-45345](https://intranet.oreilly.com/jira/browse/ITOPS-45345)) — check for that pairing rather than assuming only the SOL ticket exists.

## Capture Conventions

- Save archived reference files under `reference/` using stable, descriptive filenames.
- Use `reference/emails/` for email PDFs and add more subfolders later if other document types become common.
- Create or update a `notes/` file when the document benefits from summary, context, or cross-links.
- Add the canonical entry here so saved documents remain easy to browse.
